nextflow_workflow {

    name "Test Workflow GISTIC2_ANALYSIS"
    script "subworkflows/gistic2_analysis.nf"
    workflow "GISTIC2_ANALYSIS"

    test("Should run without failures") {

        when {
            params {
                load("$baseDir/tests/testdata/test_params.json")
            }       
            workflow {
                """
                """
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out).match()
        }

    }

}
